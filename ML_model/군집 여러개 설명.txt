1. 색상 생성 함수 추가:
   - `generate_colors` 함수를 통해 각 클러스터에 대한 고유한 색상을 생성합니다.

2. 모든 클러스터 시각화:
   - 각 클러스터마다 다른 색상을 사용하여 포인트를 표시합니다.
   - 각 클러스터의 중심점을 초록색 마커로 표시하고, 클러스터 번호를 팝업으로 추가했습니다.

3. 노이즈 포인트 처리:
   - DBSCAN에 의해 클러스터로 분류되지 않은 포인트(노이즈)를 회색으로 표시합니다.

4. 시각화 개선:
   - K-거리 그래프의 크기를 키우고 제목을 추가했습니다.

5. 결과 출력 수정:
   - 이제 모든 클러스터가 지도에 표시되므로, 특정 클러스터의 중심점만 반환하지 않습니다.
   - 대신 ε 값과 클러스터 수를 반환합니다.

이 코드를 실행하면 다음과 같은 결과를 얻을 수 있습니다:

1. K-거리 그래프: ε 값 선택의 근거를 시각적으로 제공합니다.
2. 다중 색상 지도: 각 클러스터가 서로 다른 색상으로 표시되며, 클러스터 중심점은 초록색 마커로 표시됩니다. 노이즈 포인트는 회색으로 표시됩니다.
3. 사용된 ε 값
4. 클러스터 수

이 접근 방식을 통해 데이터의 전체적인 군집 구조를 더 잘 이해할 수 있으며, 여러 개의 잠재적인 "핫스팟"을 한 번에 확인할 수 있습니다. 필요에 따라 ε 값이나 min_pts를 조정하여 클러스터링 결과를 세밀하게 조정할 수 있습니다.